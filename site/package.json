{
  "name": "axiom-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Axiom — premium product site for a faster deterministic fully dynamic maximal matching algorithm.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:links": "node scripts/check-site.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.7.3"
  }
}
